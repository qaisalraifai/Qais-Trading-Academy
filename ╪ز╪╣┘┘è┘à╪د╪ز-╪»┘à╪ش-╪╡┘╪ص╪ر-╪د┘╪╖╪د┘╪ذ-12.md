# تعليمات دمج المرحلة 12 (الدردشة) — صفحة الطالب

الملف: `app/course/[id]/CourseClient.js`

سطرين بس، بنفس مكان استيراد وعرض `BatchFilesPanel` بالضبط.

## 1) الاستيراد (فوق مع باقي الاستيرادات)

ابحثي عن هذا السطر:
```js
import BatchFilesPanel from "./BatchFilesPanel";
```

وضيفي تحته مباشرة:
```js
import BatchChatPanel from "./BatchChatPanel";
```

## 2) العرض (بمكان عرض مكتبة الملفات)

ابحثي عن هذا السطر:
```js
<BatchFilesPanel courseId={course.id} />
```

وضيفي تحته مباشرة:
```js
<BatchChatPanel courseId={course.id} />
```

هيك بس. ما في أي تعديل ثاني مطلوب بهاد الملف.
