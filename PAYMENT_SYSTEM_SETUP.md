# نظام الدفع الموحّد — دليل التشغيل (المرحلة 1: البنية المعمارية)

## شو انبنى

بنية دفع مستقلة عن أي مزوّد (Payment Gateway Agnostic)، بحيث الاشتراكات/الفواتير/
التجديدات ما بترتبط مباشرة بـ Whop أو أي بوابة تانية. راجع `lib/payments/registry.js`
للتفاصيل الكاملة عن واجهة الـ Adapter.

- **Whop** (بطاقات) — نفس السلوك الحالي بالضبط، بس هلأ ملفوف بالواجهة الموحّدة.
- **دفع يدوي USDT** — شغال بالكامل: الطالب يختار شبكة (TRC20/BEP20/ERC20)، يشوف
  عنوان المحفظة + QR، يرفع TXID وصورة إثبات، الأدمن يوافق أو يرفض من لوحة تحكم
  جديدة (`/admin/payments`)، والموافقة بتفعّل الاشتراك فوراً.
- **NOWPayments (كريبتو تلقائي)** — مسجّل بالبنية لكن **غير مفعّل ومش مربوط بأي
  مفاتيح حقيقية بعد**، متل ما طلبت بالضبط. تفعيله لاحقاً = تعبئة adapter واحد
  (`lib/payments/adapters/nowpayments-adapter.js`) + تفعيله من لوحة الأدمن، بدون
  أي تعديل على باقي النظام.

## خطوات التشغيل (بالترتيب)

### 1) شغّل الـ SQL على Supabase
افتح **Supabase → SQL Editor** ونفّذ ملف `migration-payment-gateway-architecture.sql`
كامل. آمن 100% يتنفّذ على قاعدة بيانات فيها بيانات حقيقية — ما بيلمس أي جدول أو
عمود موجود، فقط بيضيف جداول وعمود واحد جديد (`profiles.preferred_payment_provider`).

### 2) أنشئ Storage bucket
بـ Supabase → Storage: أنشئ bucket اسمه بالضبط `payment-proofs`، خاص (Private) —
لو نسيت، الكود بينشئه تلقائياً أول رفع صورة إثبات، بس تأكيد يدوي أضمن.

### 3) فعّل وسائل الدفع من لوحة التحكم
روح `/admin/payments` → تبويب "وسائل الدفع" → تأكد إن Whop و"تحويل USDT يدوي"
مفعّلين (الافتراضي أنهم مفعّلين من الـ migration، بس تأكد).

### 4) أضف محافظ USDT
نفس الصفحة → تبويب "محافظ الكريبتو" → أضف عنوان محفظة لكل شبكة بدك تدعمها
(TRC20 غالباً الأرخص عمولة وأشيع بالمنطقة).

### 5) انشر (Deploy) عادي
ما في متغيرات بيئة جديدة مطلوبة لهاي المرحلة — كل شي بيشتغل بنفس `WHOP_*` و
`SUPABASE_*` الموجودين حالياً.

## اللي بيصير تلقائياً

- أي عملية دفع (Whop أو USDT يدوي) بتحدّث نفس أعمدة `profiles` اللي كل النظام
  الحالي (middleware، الداشبورد، Discord، تقارير الأدمن) بيعتمد عليها — صفر
  تعديل على منطق الوصول (Gating).
- عمولات المسوّقين (`recordSignupCommission` / `recordRenewalCommission`)
  بتنطبق تلقائياً على أي دفعة، بغض النظر عن المزوّد.
- جدول `payments` القديم يضل يتحدّث تلقائياً (توافق كامل مع أي تقرير حالي).
- Cron يومي جديد (`/api/cron/billing`، الساعة 5 صباحاً UTC) بيدير: فواتير
  التجديد للمشتركين اللي مافي عندهم تجديد تلقائي، تذكيرات 7 أيام / 3 أيام /
  يوم الاستحقاق، وتعليق الاشتراك بعد 5 أيام فترة سماح بدون دفع.

## المرحلة الجاية (لما تجهز)

1. اختيار مزوّد كريبتو تلقائي يدعم حسابات الأردن (NOWPayments أو بديل).
2. تعبئة `lib/payments/adapters/nowpayments-adapter.js` بمفاتيح API الحقيقية.
3. تفعيله من `/admin/payments` — بس. باقي النظام (الفوترة، الإشعارات،
   العمولات، التقارير) بيشتغل معه فوراً بدون أي كود إضافي.
4. (اختياري) بوابة بطاقات بديلة لأي دولة/سبب — نفس الخطوات بالضبط: adapter
   جديد + تسجيله بجدول `payment_providers`.
