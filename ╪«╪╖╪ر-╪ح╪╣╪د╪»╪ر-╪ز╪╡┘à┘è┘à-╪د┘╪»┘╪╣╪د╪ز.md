**خطة إعادة بناء نظام الدفعات (Batch = دفعة تحتوي عدة دورات)**

Qais Trading Academy

---

## قبل ما نبدأ: شو رح يتغيّر فعليًا

هلأ النظام مبني على افتراض "دفعة واحدة = دورة واحدة" — كل جدول (الملفات، الواجبات، البث، الشهادات...) مربوط بـ `batch_id` اللي هو أصلاً مربوط بدورة وحدة.

المطلوب: الدفعة تصير كيان مستقل عن الدورة، وتقدر تحتوي أكثر من دورة. هاد مو تصميم بس — هاد تغيير **جوهر العلاقة بين الجداول**، وبالتحديد نقطتين أساسيتين:

1. **جدول الدفعات (`batches`)**: هلأ فيه عمود `course_id` يربطها بدورة وحدة. لازم يتحول لعلاقة "عدة لعدة" (دفعة وحدة فيها أكثر من دورة، ونفس الدورة ممكن تكون بأكثر من دفعة — مثلاً "الأساسيات" ممكن تكون بدفعة أغسطس وبدفعة VIP بنفس الوقت).

2. **تسجيل الطالب (`batch_enrollments`)**: هلأ الطالب يسجّل نفسه **بدفعة لكل دورة لحالها** (لأنه كانت الدفعة = دورة). لو صارت الدفعة تحتوي عدة دورات، المنطقي إن الطالب **يسجّل بالدفعة مرة وحدة**، وتلقائيًا يفتحله كل الدورات اللي جواها — مش يختار دفعة لكل دورة لحالها.

هاي نقطة لازم تأكديها لأنها بتغيّر تجربة الطالب مش بس الأدمن (شوف الأسئلة بالآخر).

---

## المرحلة 1 — تحويل الدفعة لكيان مستقل (الأساس)

**الملخص:** هاي المرحلة الأساس اللي كل شي بعدها بيبنى عليها. هون بنفصل الدفعة عن الدورة بقاعدة البيانات، وننقل بياناتك الحالية بدون ما نخسر ولا طالب أو تقدم.

**شغلي بهذي المرحلة:** أضيف جدول وسيط `batch_courses` (يربط دفعة بعدة دورات)، وأنقل كل دفعة حالية (اللي إلها `course_id` وحدة) لتصير سطر بـ `batch_courses`. بعدين أشيل الاعتماد على `course_id` من جدول `batches` نفسه (بدون ما أحذف بيانات — بس أغيّر شكل العلاقة). كل الجداول التانية (الملفات، البث، الواجبات...) ما بتتأثر لأنها أصلاً مربوطة بـ `batch_id` مباشرة.

## المرحلة 2 — تحويل التسجيل لمستوى الدفعة

**الملخص:** الطالب يسجّل بدفعة مرة وحدة، وتلقائيًا بيفتحله كل الدورات اللي جوا الدفعة، بدل ما يختار دفعة لكل دورة.

**شغلي بهذي المرحلة:** أعدّل جدول `batch_enrollments` ليصير تسجيل بمستوى الدفعة (مش لكل دورة)، وأنقل كل تسجيلات الطلاب الحالية بدون ما يحس فيها حدا. بعدها أعدّل شاشة "اختر دفعتك" (المرحلة 5 قديمًا) بحيث تظهر بمكان واحد بعد أول دورة يفتحها الطالب من أي دفعة، مش بكل دورة لحالها.

## المرحلة 3 — تعديل كل الأماكن اللي بتفلتر حسب الدفعة

**الملخص:** كل مكان بالمنصة بيسأل "شو دفعة هاد الطالب لهاي الدورة؟" لازم يتعدّل بما إن الدفعة صارت تحتوي عدة دورات.

**شغلي بهذي المرحلة:** أعدّل الدالة المركزية اللي بترجّع دفعة الطالب، والمنطق المرتبط فيها بكل الأماكن (المحاضرات، الاختبارات، الملفات، الواجبات، الدردشة، الشهادات، التقويم). ما رح ألمس تصميم ولا واجهة هون — بس أتأكد كل شي يضل يشتغل صح بعد تغيير المرحلتين 1 و2.

## المرحلة 4 — لوحة إحصائيات أعلى صفحة إدارة الدفعات

**الملخص:** شريط أعلى الصفحة يعطيكِ نظرة سريعة: عدد الدفعات الكلي، النشطة، القادمة، المنتهية، إجمالي الطلاب، والمقاعد المتبقية.

**شغلي بهذي المرحلة:** أبني الحسابات والبطاقات الصغيرة أعلى الصفحة، مع بحث وفلترة (حسب الحالة، المدرب، الدورة) وترتيب النتائج.

## المرحلة 5 — بطاقات الدفعات بدل الجدول

**الملخص:** الجدول الحالي بيتحول بالكامل لبطاقات فاخرة، كل بطاقة تلخّص دفعة وحدة بنظرة واحدة (الحالة، التواريخ، نسبة امتلاء المقاعد بشريط تقدّم، المدرب، والدورات اللي جواها كـ Tags).

**شغلي بهذي المرحلة:** تصميم البطاقة بالكامل مع الألوان حسب الحالة والحركات الانتقالية، وربطها بالإحصائيات والفلاتر من المرحلة الجاية.

## المرحلة 6 — صفحة تحكم مستقلة لكل دفعة

**الملخص:** الضغط على بطاقة الدفعة يوديكِ لصفحة كاملة خاصة فيها، بدل النوافذ المنبثقة الحالية (Modals) اللي كانت مبعثرة.

**شغلي بهذي المرحلة:** أبني صفحة `/admin/batches/[id]` فيها تبويبات (Tabs): الطلاب، البث المباشر وتسجيلاته، الملفات، المحاضرات، الواجبات، الاختبارات، الإعلانات، التقويم، الحضور، الشهادات، وإعدادات الدفعة. أغلب هاي الأقسام موجودة أصلًا كنوافذ منبثقة بالصفحة الحالية — هون بس بجمّعها بمكان واحد منظّم بدل ما تكون متفرقة.

## المرحلة 7 — Wizard إنشاء دفعة جديدة (6 خطوات)

**الملخص:** بدل نموذج طويل بمرة وحدة، إنشاء الدفعة يصير بخطوات واضحة: الاسم ← الدورات ← المدرب/المدربين ← التواريخ والمواعيد ← المقاعد وحالة التسجيل ← مراجعة نهائية.

**شغلي بهذي المرحلة:** أبني الـ Wizard بكل خطواته، وأضيف إمكانية اختيار أكثر من مدرب للدفعة الوحدة (ميزة جديدة، حاليًا كل دفعة مدرب واحد بس).

## المرحلة 8 — كل إجراءات إدارة الدفعة

**الملخص:** كل الأزرار اللي طلبتيها بمكان واحد منظّم داخل صفحة الدفعة: تعديل، حذف نهائي، أرشفة، نسخ الدفعة كاملة، فتح/إغلاق التسجيل، بدء بث، إنهاء الدفعة، إضافة/إزالة دورات، تغيير المدرب، نقل طلاب، إشعار جماعي لكل طلاب الدفعة، وتصدير قائمة الطلاب.

**شغلي بهذي المرحلة:** أغلب هاي الإجراءات (تعديل، أرشفة، نسخ، فتح/إغلاق تسجيل، بدء بث، نقل طلاب) موجودة أصلًا بالخلفية من مراحل سابقة — بس بربطها بالتصميم الجديد. اللي جديد كليًا: حذف نهائي، إنهاء الدفعة، إضافة/إزالة دورات لدفعة موجودة، تغيير المدرب، إشعار جماعي، وتصدير قائمة الطلاب (CSV).

---

## قبل ما أبدأ فعليًا، تأكدي لي من 4 نقاط:

1. **الطالب يسجّل بالدفعة مرة وحدة وبتفتحله كل دوراتها تلقائيًا** (مش يختار دفعة لكل دورة لحالها زي هلأ). موافقة؟

2. **نفس الدورة ممكن تكون بأكثر من دفعة بنفس الوقت** (مثلاً "الأساسيات" بدفعة أغسطس وبدفعة VIP سوا). موافقة؟

3. **دفعتك الحالية (اللي طلابك مسجّلين فيها هلأ) بتنتقل تلقائيًا** لتصير "دفعة تحتوي دورة وحدة" بالنظام الجديد — بدون ما يحس فيها أي طالب أو ينخسر تقدمه. موافقة؟

4. هاد مشروع كبير فعليًا (تغيير جوهر البيانات + 8 مراحل بناء). بدك ياه:
   - **دفعة وحدة كبيرة** (كل المراحل مع بعض بتسليم وحدة)، أو
   - **مرحلة مرحلة** زي الخطة فوق بالضبط (كل مرحلة تسليم لحالها تقدري تجربيها قبل ما نكمل التالية)، متل ما اشتغلنا بالمشروع الأول بالظبط؟

إذا الكل تمام، جاهز أبدأ بالمرحلة 1.
