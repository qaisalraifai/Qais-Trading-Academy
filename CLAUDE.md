# Qais Trading Academy

منصّة تعليم وتداول — Next.js 14 (App Router) · React 18 · Tailwind · Supabase · lucide-react.
الواجهة عربية RTL. الشغل الجاري: إعادة بناء **Qais Market Intelligence** كمحرك تحليل حقيقي.

---

## منهجية QAIS SK — قرارات صاحب المنهجية، مش اجتهاد

هاي قواعد محسومة. **لا تغيّرها ولا تعيد تفسيرها** بدون قرار صريح منه.

### أحداث الهيكل: نوعان فقط

- **BOS** = إغلاق جسم خلف آخر سوينغ خارجي مؤكَّد **بنفس اتجاه** الهيكل القائم. استمرار.
- **MSS** = إغلاق جسم خلف **السوينغ الحامي** للاتجاه = تغيّر اتجاه **مباشرة**.
  - صاعد: `HH → HL → HH` ← كسر الـHL الحامي = MSS هابط.
  - هابط: `LL → LH → LL` ← كسر الـLH الحامي = MSS صاعد.

**ما في CHOCH** — لا كنوع، ولا كمرحلة وسيطة، ولا كشرط. وما في MSS بيحتاج تأكيد بعده.

### قواعد صارمة

- **مستوى الحدث = سعر السوينغ المكسور.** ولا مرة من شمعة الكسر. شمعة الكسر لحظة التأكيد وبس.
- **الكسر بالإغلاق مش بالذيل.** التجاوز بالذيل بيروح لقائمة `wickBreaks` منفصلة (مادة الـSweep) وما بيولّد حدث.
- **ما في نظر للمستقبل.** المستوى ما بينكسر قبل لحظة تأكيده كسوينغ خارجي (`confirmedAtIndex`).
- **الهيكل المؤكَّد ما بيتغيّر.** ممنوع يظهر حدث وبعدين يختفي مع وصول شموع جديدة.
- **كل عتبة بالـATR** — مش مبلغ سعري ثابت. هيك بتتأقلم مع الرمز والفريم لحالها.

### الدخول (طبقات لسا ما انبنت)

الدخول **بس** من كتلة أوامر، وبعد ما السعر يلمسها ويصير SMT. ما في دخول بحالات تانية.
الأهداف بتنسقط من النقطة B بنسب: `1.000 · 1.618 · 1.809 · 2.000`.
كتلة الأوامر بتنلغى لما يسكّر السعر خلف آخر مستوى فيها.

---

## انضباط القياس — الأهم بالمشروع

هالمشروع عبر بجولات كتيرة من «المحرك يبدو صحيح» قبل ما يوصل لأرقام. القواعد اللي طلعت من هناك:

1. **ما ينكتب رقم ما إله مرجع.** أي مقياس مش قابل للقياس بيطلع `INSUFFICIENT_DATA` مع سبب — مش نسبة تقديرية.
2. **أداة القياس بتنفحص قبل ما نصدّق أرقامها.** مُصحِّح فيه خلل بيعطي دقة مقنعة وغلط.
3. **ممنوع تعيير المحرك ليطلع الرقم أحلى.** أول بنعرف ليش الرقم منخفض، وبنصنّف: خطأ برمجي / تعريف قاعدة / بيانات / التباس بالمرجع / سلوك متوقع / دليل غير كافٍ. بعدها بس بنقرر.
4. **بيانات مصطنعة بتأكّد الافتراض بدل ما تختبره.** صار فعلياً: مولّد شموع خطي طلّع تعادلات بنقاط الانعطاف → صفر بيفوت → ١٤ اختبار «نجحوا» على مصفوفات فاضية. أي مولّد لازم يستخدم عشوائية ثابتة البذرة، وكل اختبار لازم يتأكد إنه طلّع البنية اللي بيدّعي فحصها قبل ما يفحصها.
5. **`qais_ai_trades` مش ground truth** — مصدره محرك سابق فيه أخطاء مثبتة. المرجع الوحيد هو تسميات بشرية على عيّنة مجمّدة.

---

## وين وصل الشغل

### محرك الهيكل — مبني ومعيَّر

`lib/qais/structure/` — مستقل، بدون تبعيات، مقياسان (داخلي/خارجي)، تصنيف زخم متعدد العوامل.
`lib/qais/structure/verify/` — هارنس تحقق: تشغيل شمعة-بشمعة، استنتاج حجم التِك من شبكة السوق، مسح حالات حدّية، مُصحِّح ثنائي الاتجاه مقابل المرجع البشري.

**عيّنة مجمّدة + مرجع بشري**: `verify/fixtures/nas100-h4-2026.json` (٣٠٠ شمعة H4، مبصومة SHA256) و`.reference.json`.
⚠️ **ممنوع تعديل العيّنة** — أي تغيير بيبطّل التسميات المبنية عليها.

**آخر قياس** (عتبة `majorAtrMult = 3.0`):

```
الأحداث   Recall 0.75 · Precision 0.375 · F1 0.60 · FN 0
          MSS recall 1.00 · هوية السوينغ المكسور 0.75
السوينغات Recall 0.909 · دقة التسمية 0.875 · كثافة 1.82×
السلامة   صفر خرق سببية · صفر حدث غير مستقر
```

العتبة `3.0` معيَّرة على **٣ أدلة مستقلة**: المرجع البشري (١١ سوينغ)، وتوافق H4 مع هيكل D1 على ناسداك (١٠٨ بيفوت) واليورو (٩٢ بيفوت) — بلا تسمية بشرية. الاختيار على **طرف هضبة مسطّحة** مش على ذروة.

### طبقة البيانات

`lib/market-data/timeframe-consistency.js` — حارس بيفحص إذا الشمعة الكبيرة بتحتوي شموعها الصغيرة. موصول بمخرج `analyzeSymbol` كـ`dataQuality` وظاهر بالرادار كشريط تحذير.

**عيب معروف — الذهب:** شموع H4 وD1 من عقود مختلفة. ١.٦٪ بس من الأيام متطابقة. السبب: Dukascopy بترجّع **HTTP 429** على الطلبات الكبيرة (`count>1500`) فبيتراجع ليوهو `GC=F`، ويوهو بيخدم عقود مختلفة للحظي واليومي. و`yahooSpot: "XAUUSD=X"` بـ`assets.js` **رمز ميت (404)**. الفضة `SI=F` أسوأ (خطأ ١.٤٩٪).

### الترتيب المتفق عليه

`Structure → Liquidity → Displacement → OB → POI → Matryoshka → Scenario → AI → UI`

**الدقة بوابة**: ما ننتقل لطبقة قبل ما تتقاس اللي قبلها.

⚠️ **Matryoshka مش موجودة بالكود** رغم إنها بالتوثيق — لازم تتبنى من الصفر.
⚠️ **ما في أي تكامل LLM بالمشروع.** زر «حلّل بالذكاء الاصطناعي» بيعيد تشغيل محرك حتمي، و`aiConfidence` مجموع موزون ثابت بـ`decision.js` — مش ثقة نموذج.

---

## تشغيل

```bash
npm run build
node --test lib/qais/structure/structure.test.js lib/qais/structure/verify/*.test.js lib/market-data/*.test.js
```

٩٦ اختبار، بـ`node --test` المدمج. **ما في test runner ولا أي تبعية اختبار** — لا تضيف وحدة.
الجذر `commonjs` بينما `lib` ESM، فمجلدات المحرك فيها `package.json` صغير `{"type":"module"}` حتى يشتغل `node --test`.

**الشموع**: `/api/replay-candles` (Dukascopy → TwelveData → Yahoo). خلّي `count ≤ 900` بالفحص اليدوي — أكبر من هيك بينرفض ويتراجع لمصدر أضعف.

**تقرير التحقق**: `/api/structure-verify?symbol=NAS100&tf=h4,daily&count=3000&replay=800` (بده تسجيل دخول).

---

## قيود

- لا تغيّر business logic ولا routing ولا database إلا بطلب صريح.
- لا تكسر الـresponsive.
- الواجهة عربية RTL — استخدم الخصائص المنطقية (`inline-start`) مش `left/right`.
- أيقونات Lucide فقط، بدون إيموجي بالواجهة. (استثناء وحيد: تفاعلات البث المباشر.)
- Commit/push بس لما يُطلب.
