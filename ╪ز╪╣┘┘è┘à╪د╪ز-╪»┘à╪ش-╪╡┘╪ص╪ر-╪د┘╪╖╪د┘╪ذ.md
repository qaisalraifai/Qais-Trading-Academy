# تعليمات دمج المرحلة 11 (الواجبات) بصفحة الدورة للطالب

الملف: `app/course/[id]/CourseClient.js`

## 1) أضيفي الـ import
دوري على السطر:
```js
import BatchFilesPanel from "./BatchFilesPanel";
```
وضيفي تحته:
```js
import BatchAssignmentsPanel from "./BatchAssignmentsPanel";
```

## 2) أضيفي الكومبوننت
دوري على السطر:
```jsx
<BatchFilesPanel courseId={course.id} />
```
وضيفي بعده مباشرة:
```jsx
<BatchAssignmentsPanel courseId={course.id} />
```

هيك بس. الكومبوننت (BatchAssignmentsPanel.js) نفسه جاهز بنفس المجلد، ما محتاج أي تعديل عليه.
